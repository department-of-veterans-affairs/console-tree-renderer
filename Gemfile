source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

# Specify your gem's dependencies in tree-renderer.gemspec
# Bundler will automatically add this gem to a group called “development”
# To load these gems with the following line:
# Bundler.require(:default, :development)
gemspec
